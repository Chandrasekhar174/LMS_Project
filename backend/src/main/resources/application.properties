
# Configure database via environment variables (provided by Render/Railway)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/lmsdb?createDatabaseIfNotExist=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Ratnakar@8342}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# server port (Render provides PORT env var)
server.port=${PORT:8080}
